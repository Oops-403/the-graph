type RankClaimedEntity @entity {
  id: ID!
  # address: Address,
  logIndex: BigInt,
  transactionLogIndex: BigInt,
  # logType: string | null,
  # block: Block,
  # transaction: Transaction,
  # parameters: Array<EventParam>,
  # receipt: TransactionReceipt | null,  

  transaction_hash: Bytes
  user_address: Bytes # from
  contract_address: Bytes # to
  # transaction_fee: BigInt

  mint_address: Bytes # address
  mint_term: BigInt
  mint_rank: BigInt
}
