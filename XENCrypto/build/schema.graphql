# event.receipt.blockHash
# event.receipt.blockNumber
# event.receipt.contractAddress
# event.receipt.cumulativeGasUsed
# event.receipt.gasUsed
# event.receipt.logs
# event.receipt.root
# event.receipt.logsBloom
# event.receipt.transactionHash
# event.receipt.transactionIndex
# event.receipt.status
type ReceiptEntity @entity {
  id: ID!
  blockHash: Bytes
  blockNumber: BigInt
  contractAddress: Bytes
  cumulativeGasUsed: BigInt
  gasUsed: BigInt
}

# address: Address,
# logIndex: BigInt,
# transactionLogIndex: BigInt,
# logType: string | null,
# block: Block,
# transaction: Transaction,
# parameters: Array<EventParam>,
# receipt: TransactionReceipt | null,  
type RankClaimedEntity @entity {
  id: ID!

  transactionHash: Bytes
  transactionFee: BigInt

  userAddress: Bytes # from
  platformAddress: Bytes # to

  mintAddress: [String!]!
  mintTerm: BigInt
  mintStartRank: BigInt
  # isClaimed: Boolean
}
